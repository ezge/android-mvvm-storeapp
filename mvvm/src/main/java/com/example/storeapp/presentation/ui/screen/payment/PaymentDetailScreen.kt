package com.example.storeapp.presentation.ui.screen.payment

@Composable
fun PaymentDetailScreen(transactionId: String) {
    // Replace with real detail logic
    Column(modifier = Modifier.padding(16.dp)) {
        Text("Details for transaction: $transactionId")
    }
}
